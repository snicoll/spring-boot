plugins {
	id 'org.springframework.boot'
	id 'org.springframework.boot.aot'
	id 'java'
}

dependencies {
	implementation project(":library")
}

task('bootAotClasspath') {
	doFirst {
		tasks.findByName('bootAot').classpath.files.each { println it }
	}
}
